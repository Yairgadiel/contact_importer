{
  "name": "contact-importer",
  "version": "1.0.0",
  "private": true,
  "description": "PWA + Vercel serverless: scan Hebrew photos, map detected columns to contact fields, export vCard.",
  "scripts": {
    "start": "vercel dev --yes",
    "deploy": "vercel --prod"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "vercel": "latest"
  }
}
